# Tkinter Multi-Window

A simple Python GUI application built using Tkinter that demonstrates how to open a second window using the `Toplevel()` widget.

## 📌 Features

- Main window with a button
- Opens a second window on button click
- Custom window title and size
- Basic styling with background colors
